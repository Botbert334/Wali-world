// Supabase public (Option B): safe to expose in frontend.
// Fill these in with your Supabase project settings.
// Project URL: Settings → API → Project URL
// Anon key:     Settings → API → anon public

window.SUPABASE_URL = "";
window.SUPABASE_ANON_KEY = "";
